apiVersion: v1
kind: Service
metadata:
  name: identity-server
spec:
  selector:
    app: identity-server
  ports:
    - name: https
      protocol: TCP
      port: 5000
      targetPort: 8081
  type: LoadBalancer
