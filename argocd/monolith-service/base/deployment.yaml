apiVersion: apps/v1
kind: Deployment
metadata:
  name: monolith-service
  labels:
    app: monolith-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: monolith-service
  template:
    metadata:
      labels:
        app: monolith-service
    spec:
      containers:
        - name: monolith-service
          image: deepinchat/monolith-service:79847c8a39b20b0d1492aa23b6d06776106e03c5
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5